# 全局规则

## 语言设置

与用户沟通时使用简体中文

## 目录规划

- `./output/`：本次任务的**最终交付物**（报告、产物、导出文件等）。
- `./scripts/`：任务过程中编写的脚本，可复用或留作记录。
- `./tmp/`：中间过程的临时文件，任务结束可清理。

## 工作约定

- 任务交付物一律放到 `output/`，命名应能清晰表达内容（如 `report-xxx.md`、`demo-xxx.html`）。
- 临时文件放到 `tmp/`，避免污染根目录。
